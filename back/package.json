{
  "name": "ticket-window-back",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "server": "json-server --watch ./db.json --port 8080",
    "start": "node build/index.js",
    "build": "tsc -b",
    "dev": "ts-node-dev --respawn --debug --transpile-only src/index.ts"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "json-server": "^0.16.3",
    "socket.io": "^4.1.3"
  },
  "devDependencies": {
    "@types/cors": "^2.8.12",
    "@types/express": "^4.17.13",
    "@types/json-server": "^0.14.4",
    "@types/socket.io": "^3.0.2",
    "prettier": "^2.3.2",
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.3.5"
  }
}