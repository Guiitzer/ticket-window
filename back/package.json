{
  "name": "ticket-window-back",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "server": "json-server --watch ./db.json --port 8080"
  },
  "dependencies": {
    "json-server": "^0.16.3"
  },
  "devDependencies": {
    "prettier": "^2.3.2"
  }
}